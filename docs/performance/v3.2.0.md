# Performance Benchmark — v3.2.0

> Added API Path Versioning: `versionDispatcher`, `api.slothlet.versioning.*`, version metadata, dispatcher proxy. Also includes lazy-mode shutdown race fix.  
> Benchmark date: April 14, 2026  
> Node.js runtime with `--conditions=slothlet-dev`

## Summary

| Metric | Eager | Lazy | Winner | Delta |
|--------|-------|------|--------|-------|
| **Startup** | 51.24ms | 37.18ms | Lazy | 1.4x faster |
| **Function Calls** | 19.09μs | 17.09μs | Lazy | 1.1x faster |
| **Materialization** | — | 1.09ms | — | one-time |

## Startup Time (10 iterations)

```
Eager Mode:  51.24ms (avg) | 35.57ms (min) | 125.44ms (max)
Lazy Mode:   37.18ms (avg) | 28.03ms (min) |  46.89ms (max)
```

## Function Call Performance (100 iterations)

```
Eager Calls:         19.09μs (avg) | 11.01μs (min) | 150.06μs (max)
Lazy Subsequent:     17.09μs (avg) | 11.21μs (min) |  87.64μs (max)
Lazy First Call:      1.09ms (materialization)
```

## Complex Module Access (100 iterations)

```
Eager Nested:    40.74μs (avg)
Lazy First:     133.47μs (materialization)
Lazy Subsequent:  9.15μs (avg)
```

## Per-Module Breakdown (50 iterations)

| Module | Eager (avg) | Lazy First | Lazy Sub (avg) |
|--------|-------------|------------|----------------|
| math | 9.89μs | 92.99μs | 12.91μs |
| string | 58.28μs | 99.51μs | 6.26μs |
| funcmod | 4.84μs | 70.16μs | 2.59μs |

## Notes

- The versioning system adds VersionManager initialization and dispatcher proxy infrastructure. This increases startup time slightly (~2ms eager, ~2ms lazy vs v3.1.0).
- Call times for the standard benchmark are higher this run but within run-to-run variance. The benchmark does not exercise versioned paths — the overhead seen is noise, not version dispatch cost.
- The string module eager avg (58.28μs) is an outlier likely caused by a GC pause during that specific test iteration.
