# Slothlet v3.16.0 Changelog

**Release Date**: September 2026
**Release Type**: Minor
**Branch**: `release/3.16.0`

---

## Overview

Version 3.16.0 adds **stackable lifecycle routines** (#341): a `routines` config option that stacks every module's contribution to a named lifecycle-hook-style export (`initialize`, `shutdown`, or any custom name) into one callable at its composed api path, plus a root cascade (`self.<name>()` ≡ `api.slothlet.<name>()`) that runs every matching contribution anywhere in the tree. Slothlet's ordinary recursive-merge silently kept only the first writer whenever two or more modules exported the same key at the same path — correct for regular leaves, wrong for a hook every contributing module wants to run.

This replaces the internal `_collectLifecycleHooks` walk entirely — the mechanism behind the now-deprecated `collectLifecycleHooks` option — with one general-purpose system that covers `shutdown`/`destroy` auto-invocation (its old job) plus arbitrary named routines, startup invocation, and multi-contributor stacking (both new).

No public API is removed and no existing behavior changes unless a project opts in — either by setting `routines` explicitly, or by turning on `autoRoutines`/`collectLifecycleHooks`. See **Deprecations** below for one non-obvious behavior change that _does_ apply automatically to existing `collectLifecycleHooks: true` consumers.

It also adds **`stackRoutines`** (#365) — an explicit config flag, decoupled entirely from `collisionMode`, that controls whether a routine's same-path contributors that _lost_ an ordinary collision still participate in the stack/cascade — and lands a large **collision, ownership, and routine-stacking correctness pass** (#366) alongside it: collision-mode overrides are now threaded consistently through every internal `api.add()`/nested/lazy decision, speculative ownership/routine registration is reverted whenever a candidate is rejected or throws, both sides of a lazy collision are force-materialized before they're reconciled, and stacked callables are rebuilt after `api.remove()` as well as `api.add()`. The stale per-path-stack window that this changelog previously listed only as a _Known Limitation_ (#362) is now **fixed** — the stack self-heals reactively — and `assignToApiPath` correctly awaits the now-async collision reconciliation it was firing unawaited (#369). See **Bug Fixes** below.

This release also raises the minimum Node version to **`>=22.12.0`** (from `>=22.0.0`) to match the toolchain's actual floor — see **Upgrade notes**.

Also closes out the live runtime's remaining unpinned deferred-callback boundaries: `requestAnimationFrame`/`cancelAnimationFrame`, `requestIdleCallback`/`cancelIdleCallback`, IDL `on*` handler properties on `EventSource`/`WebSocket`/`Worker`/`MessagePort`/`FileReader`/`XMLHttpRequest`, and the `MutationObserver`/`ResizeObserver`/`IntersectionObserver` constructors are now all identity-pinned the same way `setTimeout` and `EventTarget.addEventListener` already were — see **Bug Fixes** below.

---

## 🐛 Bug Fixes

### `requestAnimationFrame` pinned in scheduler-context boundary patching (#349, #350)

`enableSchedulerPatching()` patched `setTimeout`, `setInterval`, `setImmediate`, `queueMicrotask`, and `process.nextTick` to pin the calling module's identity at scheduling time and restore it when the callback runs, but not `requestAnimationFrame`. A live-runtime browser-mode module that deferred its first `self`/`context` touch into an `rAF` callback — a common layout-thrash-avoidance pattern — got no identity restored and hit `RUNTIME_NO_ACTIVE_CONTEXT_SELF`, the same failure the timer patches exist to prevent for `setTimeout`. `requestAnimationFrame` is now added to `enableSchedulerPatching()`'s entry points, guarded for host absence the same way `setImmediate`/`process.nextTick` already are; `cancelAnimationFrame` needs no wrapper of its own since its handle passes straight back untouched, matching how `clearTimeout`/`clearInterval` already work off a patched `setTimeout`/`setInterval` handle. Fixed in #350 (from #349).

### `requestIdleCallback`, IDL `on*` handler properties, and Observer constructors pinned (#352, #353)

A follow-up audit of every place a live-runtime callback can be scheduled without going through a patched boundary turned up three more gaps, each a different shape:

- **`requestIdleCallback`/`cancelIdleCallback`** — same bucket as `requestAnimationFrame`; added to `enableSchedulerPatching()` with the identical guard-for-absence and handle-passthrough treatment.
- **IDL `on*` handler properties** — `eventtarget-context.mjs` only patched the `addEventListener` method; assigning `thing.onmessage = fn` writes straight to the interface's own accessor and bypassed it entirely. Since there's no single `EventTarget.prototype.onX` (each interface declares its own `on*` attributes independently), a new `src/lib/helpers/eventtarget-property-context.mjs` patches each interface's on* accessor by name — `EventSource`, `WebSocket`, `Worker`/`MessagePort`, `FileReader`, `XMLHttpRequest` — tracking the pinned wrapper per target in a `WeakMap` so the getter still returns the exact function assigned. `HTMLElement`'s `on*` attributes (`onclick`, …) are deliberately left unpatched since `addEventListener` already covers DOM listener registration.
- **Observer constructors** — `MutationObserver`/`ResizeObserver`/`IntersectionObserver` take their callback as a constructor argument, a third boundary shape neither existing patch covered. A new `src/lib/helpers/observer-context.mjs` wraps all three constructors with `Reflect.construct` so the callback is pinned before construction, while `instanceof`, subclassing, and static extras keep working the same as unpatched; the wrapper constructor gets its own prototype linked to the original's rather than sharing it verbatim, so `instance.constructor` reflects the patched global instead of the unpatched original.

Hardening from review: `wrapperSet` (the on\* property patch) now mutates its tracked `WeakMap` entry only after the platform setter succeeds, so a throwing assignment can't leave the tracked entry out of sync with what's actually installed, and `wrapperGet` deletes the tracked entry when the platform no longer reports the wrapper it installed rather than leaving a stale pair reachable for the target's lifetime; a non-configurable on\* accessor is now skipped instead of aborting the whole patching pass. Fixed in #353 (from #352).

### Stale per-path routine stack now self-heals reactively (#362, #374)

Earlier in this release's development the per-path stacked callable was only (re)installed at three points — the end of the initial compose, after every `api.add()`, and immediately before an auto-fired `startup`/`shutdown`/`destroy` cascade. An ordinary property touch or a late direct `self.x.y = fn` reassignment was captured into the routine registry but did not re-trigger a rebuild, so when two or more contributors collided on the exact same composed api path and the second arrived after the last rebuild, the live property reflected only the plain merge winner — silently dropping the other contributor until some unrelated later event rebuilt the stack. (This changelog previously carried it as a _Known Limitation_.)

`onImplCreated` now reactively re-derives just the affected path's callable the moment a raw capture makes it a genuine multi-contributor stack, reusing `rebuildStacks()`'s own per-path install so it matches exactly what a full sweep would leave — including the "last-registered-routine-wins" precedence when two distinct routines resolve to the same path, and the cascade-slot case where a routine's own name coincides with a root-level contributor's path (that slot stays cascade-owned, and the root cascade now forwards its arguments to every contributor tree-wide rather than dropping them). The reactive write is deferred to a macrotask so it can never race the framework's own (now-async, see #369) assignment for the same path, and it no-ops entirely while any build/add/reload is in progress — that operation's own terminal `rebuildStacks()` is the authority there. Modules mounted via `api.modules.addModule()`/`addModules()` now rebuild stacks after mounting too, closing a path that previously installed none. Fixed in #374.

### `assignToApiPath` awaits the now-async collision reconciliation (#369, #376)

`ApiManager.syncWrapper` became async when it started force-materializing both sides of a collision (see the collision-correctness pass below), but `ApiAssignment.assignToApiPath` — a synchronous function — was still calling it without awaiting, then returning success before the materialization, child-key transfer, and shadow bookkeeping had actually run, and letting any error thrown inside it surface as an unhandled promise rejection. `assignToApiPath` is now `async` and awaits `syncWrapper`, and the `await` is threaded through every one of its callers across `api-assignment.mjs`, `api-manager.mjs`, and `modes-processor.mjs` (including the internal `#assignWithRoutineRevert` loop, whose `if (!assigned)` revert would otherwise have tested a Promise — always truthy — and never reverted a rejected candidate). Fixed in #376.

### Collision, ownership & routine-stacking correctness hardening (#366, #367, #372)

Building `stackRoutines` surfaced a broad set of pre-existing correctness gaps in the collision-resolution, ownership-tracking, and routine-capture machinery, fixed as one pass:

- **Collision-mode threading.** `collisionModeOverride` and the originating `collisionContext` are now threaded into every nested, lazy, and default+named-export merge decision instead of silently falling back to the instance default; the `"addApi"`/`"add"` collisionContext mismatch that masked two tests is corrected (#367); and the base-API cache entry stores `collision.initial` rather than a stale mode.
- **Speculative registration is fully reverted on rejection.** When an internal `api.add()` candidate is skip/warn-rejected or throws, its speculative ownership registration, its `RoutineManager` raw capture, and its own (and its subtree's) wrappers are all reverted/invalidated — previously a rejected candidate could leave a stale owner, a phantom routine contributor, or an in-flight background materialization that still re-applied its result.
- **Ownership merge-loss precedence.** `isMergeLoss` is corrected for object-vs-function merges and replace precedence, an authoritative re-registration no longer flags the current winner as a loser, the real leaf-merge winner stays the current owner rather than last-registered, and merge-loss is tracked on the entry itself rather than by stack position (#372).
- **Lazy-collision reconciliation.** Both sides of a lazy collision are force-materialized before `syncWrapper` reads their child keys, so a still-lazy side can no longer make the "does this key already exist" decision on incomplete information and silently discard a sibling; `syncWrapper`'s final bookkeeping no longer misreads a callable impl as unmaterialized.
- **Routine capture robustness.** A stacked callable's raw entries are filtered by its own routine (not apiPath alone), a self-referential capture that could recurse to OOM is pruned (#372), raw-entry order is preserved on restore, a root-level `api.add("", folder)` module's routines now match, and `boundApi`'s own mirror no longer misreads its own not-yet-visible write as a foreign collision.

## ✨ Features

### Stackable lifecycle routines (#341)

- **Config**: `routines: Array<string | { name, mode?, recursive?, order? }>`. String forms: `"name"` (mode `"manual"`) or `"name:mode"`. Object form adds `recursive` (boolean, default `false`) and `order` (`"mount" | "depth"`, mode-defaulted). Providing `routines` at all **replaces** the built-in default (`slothlet.defaults.routines`: `initialize` → `startup`, `shutdown` → `shutdown`) — spread `slothlet.defaults.routines` to extend rather than replace, or pass `[]` to disable every routine. A routine name may not be `"slothlet"` or a prototype-pollution meta-key (`__proto__`, `constructor`, `prototype`) — both are rejected at config-validation time, since a routine's name is used verbatim as a property key on the api root and `api.slothlet`.
- **Name matching — three modes, all reusing slothlet's existing glob engine (`compilePattern()`)**:
  - **Bare / dotted, mount-relative (default)** — `name` is resolved relative to the module's own mount point (a `dir`-scan top level, or an `api.add()` target). A bare name (`"initialize"`) matches only that mount's own top level; a dotted name (`"admin.initialize"`) matches a fixed relative sub-path. A mount's own top level is always eagerly constructed regardless of lazy/eager mode, so the common case never forces materialization.
  - **`recursive: true`** — the dotted/bare name is searched at _any_ depth within the mount's own subtree (`**.<name>` under the mount), not just the fixed relative path — including a subtree whose own top-level folder happens to be literally named `shutdown`, `destroy`, or `slothlet`, since those names are reserved only at the true api root, never at a mount's own top level.
  - **`^`-prefixed, root-anchored** — the name is a full glob (`*`, `**`, `{a,b}`, `!`) matched against the complete composed api path, crossing mount boundaries (e.g. `^ext.*.initialize`, `^**.shutdown`).
  - Recursive and root-anchored matches force lazy-mode materialization of the relevant subtree (the whole tree, for a root-anchored pattern) before a cascade runs — the deliberate, unavoidable crux of combining lazy mode with a routine name that isn't mount-top-level. That materialization is scoped to what the pattern actually needs: when a mount's contents are already covered by the base compose's own subtree, they aren't walked a second time.
- **Modes**: `manual` (never auto-runs; always stacked/callable), `startup` (cascade runs once, at the end of `load()` / `await slothlet(...)`), `shutdown` (cascade runs on `api.shutdown()` / `api.slothlet.shutdown()`), `destroy` (cascade runs on `api.destroy()`, which also runs the root `shutdown()` afterward — so a `mode: "shutdown"` routine still runs as part of `destroy()` too; `destroy` covers only what a consumer wants to fire specifically on destroy). `destroy()`'s own teardown (marking the instance destroyed, clearing api keys, nulling `slothlet.api`) always completes even when a routine fails during either its `destroy`-mode cascade or the `shutdown()` call it makes internally — any failure is deferred and rethrown only after cleanup finishes.
- **Root cascade ordering**: `order: "mount"` (default for `startup`/`manual`) runs groups in registration order; `order: "depth"` (default for `shutdown`/`destroy`) runs deepest composed-path groups first, mirroring the old walk's deepest-first teardown order.
- **Errors — best-effort, aggregated**: a throwing contributor does not stop the chain — every contributor at a path, and every path in a cascade, still runs, and a receiver that can't be resolved (e.g. a permission-gated read) is treated like a missing one rather than aborting the run. If any contributor failed, one aggregated `ROUTINE_FAILED` error is thrown afterward (`context.count`, `context.failures: [{apiPath, moduleID}, …]`, `cause` = the first failure) — matching `module-manager.mjs`'s existing best-effort convention rather than introducing a second error-handling philosophy.
- **De-duplication and re-mount safety**: a module's contribution is captured once per `(apiPath, moduleID)` regardless of how many `impl:created`/`impl:changed` events it produces; removing a module drops its contributions from every affected cascade without double-counting a re-add.
- **`autoRoutines`** (new, non-deprecated replacement for `collectLifecycleHooks`): `false` by default (temporary v3-compat — see Deprecations). Every configured routine is always stacked and directly callable regardless of this flag; `autoRoutines` only gates _automatic_ firing (`startup` at compose end, `shutdown`/`destroy` at dispose).
- **`slothlet.defaults`**: new public namespace (attached on both the ESM and CJS entry points) exposing `slothlet.defaults.routines` (the frozen built-in routines list) and `slothlet.defaults.reservedExports` (the framework-reserved export names, as a genuinely immutable `Set` — reads work normally but `add`/`delete`/`clear` throw, since `Object.freeze()` alone doesn't stop a `Set`'s own mutating methods) — lets a consumer extend the defaults without retyping them.

### Multi-contributor stacking control: `stackRoutines` (#365)

- **Config**: `stackRoutines: boolean` (default `false`). It governs whether a routine's contributors that _lost_ an ordinary same-path collision still participate in that routine's stack and cascade — and it is decoupled **entirely** from `collisionMode`. Nothing before the routines feature ever ran more than one function at a shared api path, so multi-contributor stacking must be an explicit opt-in, not an implicit side effect of whichever `collisionMode` happened to be in play.
  - `false` (default) — a routine runs only the contribution that is ownership's current owner of the path, exactly matching what won the collision on the composed tree and what a direct call would invoke. Existing behavior; no change.
  - `true` — every raw-captured contribution at the path runs, uniformly, regardless of `collisionMode`. This is what makes "two modules that both export `initialize` at the same composed path each run" actually happen for a collided/merged path.
- Applies identically to the per-path stacked callable and the root cascade (both derive their contributor set through the same filter), so a direct `self.x.initialize()` and the `api.slothlet.initialize()` cascade agree on who runs.

---

## ⚠️ Deprecations

### `collectLifecycleHooks` — deprecated in favor of `routines` + `autoRoutines`

`collectLifecycleHooks` (and its internal engine, `_collectLifecycleHooks`) is removed and fully replaced by the routines system. Setting `collectLifecycleHooks: true` now:

1. Expands into two implicit, root-anchored routines reproducing the old option's exact scope for literally-named hooks: `{ name: "^**.shutdown", mode: "shutdown", order: "depth" }` and the `destroy` equivalent — whole-tree, cross-mount, deepest-first, best-effort, same as the original walk.
2. **Drops** any existing `shutdown`/`destroy`-mode routine already in the effective list — including the built-in default `shutdown` routine — before adding the two implicit ones, so a contributor is never invoked twice (once via the default bare-name routine, once via the implicit root-anchored one).
3. Sets the effective `autoRoutines` to `true`, unless `autoRoutines` is given explicitly (which always wins).

A project that only ever used `collectLifecycleHooks: true` for its documented purpose (auto-invoking nested `shutdown`/`destroy` functions) sees identical behavior for those two hooks. Emits a `V3_CONFIG_DEPRECATED` warning unless `silent: true`; will be removed in v4.

**Compatibility note — `autoRoutines` is not per-mode.** Because `collectLifecycleHooks: true` now also sets `autoRoutines: true`, and the built-in default routines list includes `initialize` (mode `startup`) alongside `shutdown`, turning on `collectLifecycleHooks` also newly auto-invokes any top-level `initialize` export at every mount's own root at the end of compose — something the old mechanism never did (it only ever touched `shutdown`/`destroy`). A project with `collectLifecycleHooks: true` and a mount-root `initialize.mjs` that was previously just an inert leaf will now have that function called automatically on `await slothlet(...)`. This is a side effect of `autoRoutines` being a single all-modes gate rather than a per-mode one, not a bug — but it is a real, non-obvious behavior change for existing `collectLifecycleHooks: true` consumers who happen to have a top-level `initialize` export they did not intend as a startup hook. (Nested/non-top-level `initialize` exports are unaffected — the default `initialize` routine is bare and mount-relative, not recursive or root-anchored.)

**Migration recipe for v4 (once `collectLifecycleHooks` is removed)** — reproduce the exact same effect explicitly:

```js
await slothlet({
	dir: "./api",
	autoRoutines: true,
	routines: [
		...slothlet.defaults.routines, // keep initialize:startup, drop or override as needed
		{ name: "^**.shutdown", mode: "shutdown", order: "depth" },
		{ name: "^**.destroy", mode: "destroy", order: "depth" }
	]
});
```

If the newly-surfaced `initialize` auto-invocation described above is unwanted, drop it from the spread (`routines: [{ name: "shutdown", mode: "manual" }, { name: "^**.shutdown", ... }, { name: "^**.destroy", ... }]` — keep `initialize` `manual` or omit it) while keeping `autoRoutines: true` for the shutdown/destroy cascades.

---

## 🧪 Tests

- **`tests/vitests/suites/config/routines-config.test.vitest.mjs`** (22 tests) — `slothlet.defaults` shape, freezing, and genuine `reservedExports` mutation-rejection; `routines` config normalization (string/object forms, `recursive`/`order` validation and defaults, meta-key/`"slothlet"` rejection, replace-not-merge semantics, `autoRoutines` interaction).
- **`tests/vitests/suites/lifecycle/routines.test.vitest.mjs`** (run in both eager and lazy mode) — bare/dotted mount-relative matching, `recursive` matching, `^`-root-anchored glob matching, flat cross-mount registration-order cascades, `startup`/`shutdown`/`destroy`/`manual` modes (including that `destroy()`'s own teardown always completes even when a routine fails), `order: "mount"` vs `"depth"`, best-effort/aggregated error propagation, `collectLifecycleHooks` interaction, plus the #362 reactive self-heal: late-contributor healing, root-name-slot healing to the cascade, cascade argument forwarding, last-registered-routine-wins precedence when the reactive patch installs the stack, and the macrotask-deferral race guard.
- **`tests/vitests/suites/lifecycle/routines-stack-flag.test.vitest.mjs`** — `stackRoutines` behavior across modes and collision modes: losers stack only when `stackRoutines: true`, and the per-path callable and the root cascade agree on the contributor set.
- **`tests/vitests/suites/modules/routine-stack-on-mount.test.vitest.mjs`** — a routine matching a leaf of a module mounted via `api.modules.addModule()`/`addModules()` gets its stacked callable installed and actually invokes the mounted contributor (#362).
- **`tests/vitests/suites/handlers/routine-manager-revert.test.vitest.mjs`** — routine raw-capture / ownership revert on rejected or throwing internal collision candidates.
- **`tests/vitests/suites/config/routines-config.test.vitest.mjs`** — extended for `stackRoutines` normalization alongside the existing `routines`/`autoRoutines`/`slothlet.defaults` coverage.
- **`tests/vitests/suites/builders/lifecycle-hooks-collect.test.vitest.mjs`** (23 tests) — the pre-existing `collectLifecycleHooks` suite exercises the new engine end-to-end through the public `api.shutdown()`/`api.destroy()` surface rather than the old internal walk.
- New fixture directories under `api_tests/`: `api_test_routines{,_auth1,_auth2,_good,_bad,_manual,_nested,_module,_cascade_args,_crosspath_root,_crosspath_mounted}`.
- Post-destroy idempotency: calling `destroy()` (or `shutdown()`) a second time after `api.destroy()` has already nulled `slothlet.api` is a no-op (`runCascade`/`runPath` return immediately).
- **`tests/vitests/suites/context/boundary-patch-helpers.test.vitest.mjs`** (45 tests) — covers all four boundary-patch fixes above: `requestAnimationFrame`/`cancelAnimationFrame` scheduling and cancellation, `requestIdleCallback`/`cancelIdleCallback` with exact prior own-property-descriptor restoration (forcing absence/presence rather than assuming the host default, and restoring via the captured descriptor rather than a bare value so a real pre-existing implementation isn't wiped for the rest of the suite), the on\* handler property wrapper across `EventSource`/`WebSocket`/`Worker`/`MessagePort`/`FileReader`/`XMLHttpRequest` (including rejected-assignment and non-configurable-accessor cases), and the three Observer constructors (`instanceof`, subclassing, constructor identity).
- Full node coverage gate green; `npm run lint` and `npm run format:check` both clean.

---

## 📚 Documentation

- **NEW:** [docs/changelog/v3/v3.16.0.md](./v3.16.0.md) — this changelog.
- [docs/LIFECYCLE.md](../../LIFECYCLE.md) — "Routines" section: config table (`recursive`/`order`), the four modes, name-matching comparison table (bare/dotted/recursive/root-anchored), stacking + root cascade ordering, best-effort/aggregated errors, `autoRoutines`, and a "Relationship to `collectLifecycleHooks`" subsection.
- [docs/CONFIGURATION.md](../../CONFIGURATION.md) — `collectLifecycleHooks` marked deprecated with its expansion behavior; `routines` section documents the final type signature; new `autoRoutines` and `stackRoutines` sections, including how the non-stacked (default) routine winner is chosen.
- [docs/MODULE-STRUCTURE.md](../../MODULE-STRUCTURE.md) — routine special-name note: contributions at the "same composed api path" stack in "registration order."
- [docs/API-RULES.md](../../API-RULES.md) — corrected `moduleID` casing and `api.add()`'s real call shape; `collisionMode`/`collisionModeOverride`/`moduleID` documented on `assignToApiPath`.
- [docs/PERMISSIONS.md](../../PERMISSIONS.md) — the live-runtime patched-boundary list and comparison table now include `requestAnimationFrame`, `requestIdleCallback`, IDL `on*` handler properties, and the three Observer constructors alongside the pre-existing timer/microtask and `EventTarget.addEventListener` entries.

---

## 🔧 CI & tooling

- **Minimum Node raised to `>=22.12.0`** (from `>=22.0.0`) to match the toolchain's actual floor — the vitest 5 line's native bindings require it (see Upgrade notes for the consumer impact). `engines.node` is a single field that binds consumers, contributors, and this repo's own CI matrix alike; the CI `min_node_version` is aligned to the same floor.
- **vitest 4 → 5** (#355) — migrated off `describe.sequential` (removed in vitest 5) and updated the runner to ignore vitest 5's default `.vitest/attachments/` attachments dir.
- Type-generation fix: the jsdoc-typescript-types plugin now handles a tuple element that is itself an array type (#363).

## 🔧 Dependencies

- `vitest` (and `@vitest/*` group) 4 → 5 (#355)
- `@eslint/css` 1.4.0 → 2.0.0 (#357)
- `playwright` 1.62.1 → 1.63.0 (#359)
- `@cldmv/fix-headers` 1.3.10 → 1.3.11 (#356)
- `@types/node` 26.4.0 → 26.4.1 (#358)
- Reuses the existing glob engine (`compilePattern()` from `src/lib/helpers/pattern-matcher.mjs`) already used by `HookManager`/`PermissionManager` for the routines feature — no new runtime dependency, internal or external.

---

## Upgrade notes

- **Minimum Node is now `>=22.12.0`** (previously `>=22.0.0`). This is the one change existing consumers must check: a project pinned to a Node 22.0–22.11 patch release must move to 22.12.0+ (or the next even LTS line). `engines.node` binds consumers, not just this repo's CI, so an install on an older 22.x will emit an `EBADENGINE` warning.
- **No API breaking changes.** `routines`, `autoRoutines`, and `stackRoutines` are all opt-in (`autoRoutines` and `stackRoutines` default to `false`); a project that never touches them sees no behavior change, including the built-in default `initialize`/`shutdown` routines — always stacked/callable but never auto-fire unless `autoRoutines: true`.
- **The #362/#369 fixes and the collision/ownership hardening are internal correctness only** — no public API or signature changes; drop-in for the previous behavior. The reactive self-heal and the awaited collision reconciliation make already-intended behavior actually hold in edge cases that previously dropped a contributor or returned before work completed.
- **`collectLifecycleHooks: true` now also implies `autoRoutines: true`** (unless `autoRoutines` is set explicitly) — see Deprecations above for the resulting `initialize` auto-invocation side effect. This is the one case existing consumers of the deprecated flag should specifically check for.
- **`collectLifecycleHooks` is deprecated**, emits `V3_CONFIG_DEPRECATED` unless `silent: true`, and will be removed in v4 — see the migration recipe above.
- **The boundary-patch fixes are also non-breaking and drop-in.** A live-runtime browser-mode module that schedules deferred work via `requestAnimationFrame`/`requestIdleCallback`, assigns an IDL `on*` handler property, or constructs a `MutationObserver`/`ResizeObserver`/`IntersectionObserver` now gets correct `self`/context identity inside that callback where it previously threw `RUNTIME_NO_ACTIVE_CONTEXT_SELF` or silently ran unattributed; no code changes are required to pick this up.
