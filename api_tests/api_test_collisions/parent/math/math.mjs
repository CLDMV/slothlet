/**
 * Nested folder-level math functions (parent/math/math.mjs)
 * These should be merged with the file-level functions
 */

export function add(a, b) {
	return a + b;
}

export function multiply(a, b) {
	return a * b;
}

export function divide(a, b) {
	return a / b;
}
