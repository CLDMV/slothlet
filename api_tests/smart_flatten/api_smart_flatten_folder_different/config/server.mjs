/**
 * Server config file inside config subfolder.
 * Tests what happens when addApi("config", folder) encounters folder/config/ with non-matching files
 */

export function getServerConfig() {
	return "server-config-data";
}

export const serverPort = 3000;
