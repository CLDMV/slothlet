Fixture: a regular file inside node_modules. discoverModules's enumeration step
should skip non-directory entries (covers the `if (!entry.isDirectory() && !entry.isSymbolicLink()) continue;` branch in enumerateNpmPackages).
