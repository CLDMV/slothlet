export namespace translations {
    let INVALID_CONFIG: string;
    let INVALID_CONFIG_DIR_MISSING: string;
    let INVALID_CONFIG_DIR_INVALID: string;
    let INVALID_CONFIG_MODE_INVALID: string;
    let INVALID_CONFIG_MODE_UNKNOWN: string;
    let INVALID_CONFIG_NOT_LOADED: string;
    let INVALID_CONFIG_RELOAD_NOT_IMPL: string;
    let INVALID_CONFIG_LAZY_NOT_READY: string;
    let INVALID_CONFIG_LAZY_ASYNC_REQUIRED: string;
    let INVALID_CONFIG_dir: string;
    let INVALID_CONFIG_mode: string;
    let INVALID_CONFIG_runtime: string;
    let INVALID_CONFIG_generic: string;
    let MODULE_LOAD_FAILED: string;
    let MODULE_NO_EXPORTS: string;
    let MODULE_IMPORT_FAILED: string;
    let MODULE_NOT_FOUND: string;
    let MODULE_LOAD_ERROR: string;
    let MODULE_INVALID_EXPORT: string;
    let CONTEXT_ALREADY_EXISTS: string;
    let CONTEXT_NOT_FOUND: string;
    let CONTEXT_EXECUTION_FAILED: string;
    let NO_ACTIVE_CONTEXT: string;
    let NO_ACTIVE_CONTEXT_LIVE: string;
    let NO_ACTIVE_CONTEXT_ASYNC: string;
    let RUNTIME_NO_ACTIVE_CONTEXT_SELF: string;
    let RUNTIME_NO_ACTIVE_CONTEXT_CONTEXT: string;
    let RUNTIME_ERROR: string;
    let OWNERSHIP_INVALID_MODULE_ID: string;
    let OWNERSHIP_INVALID_API_PATH: string;
    let OWNERSHIP_CONFLICT: string;
    let INVALID_DIRECTORY: string;
    let FUNCTION_EXECUTION_FAILED: string;
    let INTERNAL_INVALID_STATE: string;
    let INTERNAL_ERROR: string;
    let INTERNAL_INVALID_ARGUMENT: string;
    let NOT_IMPLEMENTED: string;
    let WARNING_RESERVED_PROPERTY_CONFLICT: string;
    let WARNING_LANGUAGE_LOAD_FAILED: string;
    let WARNING_LANGUAGE_UNAVAILABLE: string;
    let ERROR_RUNTIME_IMPORT_FAILED: string;
    let DEBUG_MODE_ROOT_FILE: string;
    let DEBUG_MODE_ROOT_CONTRIBUTOR: string;
    let DEBUG_MODE_ROOT_CONTRIBUTOR_APPLIED: string;
    let DEBUG_MODE_NESTED_FILE: string;
    let DEBUG_MODE_PROCESSING_DIRECTORY: string;
    let DEBUG_MODE_MODULE_DECISION: string;
    let DEBUG_MODE_FLATTENING: string;
    let DEBUG_MODE_FOLDER_MATCH: string;
    let DEBUG_MODE_FOLDER_DEFAULT: string;
    let DEBUG_MODE_DIRECTORY: string;
    let HINT_REFERENCE_REMOVED: string;
    let HINT_MODULE_NOT_FOUND: string;
    let HINT_SYNTAX_ERROR: string;
    let HINT_CONTEXT_NOT_FOUND: string;
    let HINT_CONTEXT_ALREADY_EXISTS: string;
    let HINT_CONTEXT_EXECUTION_FAILED: string;
    let HINT_NO_ACTIVE_CONTEXT: string;
    let HINT_NO_ACTIVE_CONTEXT_ASYNC: string;
    let HINT_NO_ACTIVE_CONTEXT_LIVE: string;
    let HINT_OWNERSHIP_INVALID_MODULE_ID: string;
    let HINT_OWNERSHIP_INVALID_API_PATH: string;
    let HINT_OWNERSHIP_CONFLICT: string;
    let HINT_INVALID_CONFIG: string;
    let HINT_INVALID_CONFIG_DIR_INVALID: string;
    let HINT_INVALID_CONFIG_MODE_INVALID: string;
    let HINT_INVALID_CONFIG_MODE_UNKNOWN: string;
    let HINT_INVALID_CONFIG_DIR_MISSING: string;
    let HINT_MODULE_LOAD_FAILED: string;
    let HINT_MODULE_IMPORT_FAILED: string;
    let HINT_MODULE_NO_EXPORTS: string;
    let HINT_INVALID_DIRECTORY: string;
    let HINT_INVALID_CONFIG_LAZY_NOT_READY: string;
    let HINT_INTERNAL_INVALID_STATE: string;
    let HINT_INVALID_CONFIG_LAZY_ASYNC_REQUIRED: string;
    let HINT_RUNTIME_NO_ACTIVE_CONTEXT_SELF: string;
    let HINT_RUNTIME_NO_ACTIVE_CONTEXT_CONTEXT: string;
    let HINT_INVALID_CONFIG_NOT_LOADED: string;
    let HINT_INVALID_CONFIG_RELOAD_NOT_IMPL: string;
}
export namespace metadata {
    let code: string;
    let name: string;
    let nativeName: string;
}
//# sourceMappingURL=en-us.d.mts.map